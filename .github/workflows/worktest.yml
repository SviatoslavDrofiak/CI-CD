name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Клонуємо репозиторій
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🔐 Виводимо секрет (тільки для прикладу — не використовуй у реальному CI)
      - name: Show secret (debug only)
        run: echo "Secret: ${{ secrets.SECRET_NAME_1 }}"

      # Встановлюємо залежності з requirements.txt (Python-приклад)
      - name: Install dependencies
        run: pip install -r requirements.txt

      # Виконуємо bash-команди
      - name: Run a one-line script
        run: echo Hello, world!

      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

      # Запуск юніт-тестів (pytest)
      - name: Run unit test
        run: pytest
