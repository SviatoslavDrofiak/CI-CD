name: MYCI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Клонуємо репозиторій
      - name: Checkout repository
        uses: actions/checkout@v4

      # (опціонально) Встановлення Python, Node.js або Java
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.10'

      # Встановлюємо залежності (Python-приклад)
      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # Збірка проєкту
      - name: Build project
        run: echo "Building the project..."

      # Запуск юніт-тестів
      - name: Run unit tests
        run: pytest
